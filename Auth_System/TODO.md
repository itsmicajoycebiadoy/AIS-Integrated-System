# JWT Cookie for Login Implementation

## Steps:
- [x] 1. Create TODO.md with plan steps
- [x] 2. Edit UserController.js to set JWT cookie on login
- [x] 3. Edit authHandler.js to read JWT from cookie (fallback to header)
- [x] 4. Update TODO.md upon completion
- [x] 5. Tests verified: Login sets 'jwt' cookie (httpOnly, secure if prod, 24h). Middleware reads cookie or header. (Server may need manual restart: cd Auth_System && npm start)

**Status:** ✅ COMPLETE
